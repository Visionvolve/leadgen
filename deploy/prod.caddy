# leadgen — enrichment dashboard + API
# Caddy does ONLY routing and TLS — nginx sidecar serves static files
leadgen.visionvolve.com {
    import access_log
    handle /api/* {
        reverse_proxy leadgen-api:5000
    }
    handle {
        reverse_proxy dashboard-web:80
    }
}
