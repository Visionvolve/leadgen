---
phase: 12-allow-edit-company-name-both-inline-and-from-company-detail-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .worktrees/bl1117-editable-company-name/  # created
  - docs/specs/editable-company-name.md
autonomous: true
requirements:
  - BL-1117
must_haves:
  truths:
    - "BL-1117 exists in backlog service in sprint 'Sprint 25 — LCC client requests' and is claimed by the agent with status='Building'"
    - "A worktree at .worktrees/bl1117-editable-company-name exists, checked out to branch feature/bl1117-editable-company-name based on origin/staging"
    - "docs/specs/editable-company-name.md exists with the full spec (problem, user stories, acceptance criteria, data model, API, UI wireframe)"
    - "The branch is pushed to origin with upstream tracking set"
  artifacts:
    - path: "docs/specs/editable-company-name.md"
      provides: "Editable company name + duplicate detection feature spec"
      contains: "## Problem, ## User Stories, ## Acceptance Criteria (Given/When/Then), ## Data Model Changes, ## API Contracts, ## Modal Wireframe"
    - path: ".worktrees/bl1117-editable-company-name/.git"
      provides: "Worktree directory checked out to feature branch"
  key_links:
    - from: "BL-1117 in backlog service"
      to: "feature/bl1117-editable-company-name branch"
      via: "pr field updated by Plan 09"
      pattern: "backlog_update_item.*BL-1117"
---

<objective>
Bootstrap Phase 12 execution: create the backlog item (BL-1117), claim it,
set Building status, create the feature worktree off origin/staging, write
the canonical spec to docs/specs/editable-company-name.md, and push the
empty branch.

Purpose: All downstream plans (12-02..12-09) require BL-1117 to exist and
the worktree to be in place. Spec is the single source of truth for
reviewers and the PR description.
Output: BL-1117 created+claimed+Building, worktree at
.worktrees/bl1117-editable-company-name/, spec committed and pushed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-allow-edit-company-name-both-inline-and-from-come-detail-/12-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync staging + create backlog item BL-1117</name>
  <read_first>
    - /Users/michal/git/leadgen-pipeline/.planning/phases/12-allow-edit-company-name-both-inline-and-from-company-detail-/12-CONTEXT.md (the spec source)
    - /Users/michal/git/leadgen-pipeline/CLAUDE.md (GSD ↔ Backlog Status Bridge section)
  </read_first>
  <files>none (uses MCP tools + git only)</files>
  <action>
    Step 1: From the main repo root /Users/michal/git/leadgen-pipeline run:
      git fetch origin
      git status                       # must be clean OR only show pre-existing tracked changes
    Verify branch protection: we will NOT switch the main worktree's branch.

    Step 2: Call the backlog MCP tool to create BL-1117 (do NOT hand-write JSON files):
      backlog_create_item(
        project="leadgen-pipeline",
        title="Editable company name + duplicate detection",
        priority="Should Have",
        sprint="Sprint 25 — LCC client requests",
        description=<<<
          Phase 12 of milestone v25. Allow users to edit a company's
          name field inline in the Companies table and on the Company
          detail page. On save, server normalizes (trim + lowercase +
          NFKD diacritic strip + Czech/DE/EN legal suffix strip) and
          checks for same-tenant collisions. On collision, return 409
          with match summaries; frontend opens DuplicateCompanyModal
          offering per-match 'Use this one' / 'Merge into this one',
          plus footer 'Keep both' / 'Cancel'. Merge re-points all FKs
          into the surviving record and hard-deletes the loser inside
          one Postgres transaction.

          Spec: docs/specs/editable-company-name.md
          GSD phase: 12-allow-edit-company-name-both-inline-and-from-company-detail-
          Branch: feature/bl1117-editable-company-name
        >>>,
      )

    Capture the returned short_id (expected: BL-1117). If a different
    short_id is returned, USE THAT VALUE everywhere downstream — do not
    hard-code BL-1117. Echo the short_id into the plan output for
    downstream tasks to pick up.

    Step 3: Claim and set Building:
      backlog_claim_item(<short_id>)
      backlog_update_item(<short_id>, status="Building")

    NOTE: backlog_claim_item alone does NOT change status — both calls
    are required per CLAUDE.md "Status Transitions".
  </action>
  <verify>
    <automated>
      # Confirm item exists and is claimed by this agent
      backlog_get_item(<short_id>) and assert status == "Building" and claimed_by is set
    </automated>
  </verify>
  <done>
    BL-1117 (or returned short_id) exists in sprint 25 with status='Building'
    and is claimed by this agent.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create worktree off origin/staging and push empty branch</name>
  <read_first>
    - /Users/michal/git/leadgen-pipeline/scripts/worktree.sh (first 60 lines — verify --no-claude flag exists)
    - /Users/michal/git/leadgen-pipeline/CLAUDE.md ("Branch Model" + "Multi-Worktree Setup" sections)
  </read_first>
  <files>.worktrees/bl1117-editable-company-name/ (new directory)</files>
  <action>
    Run the worktree script from the main repo root, passing --no-claude
    so it does NOT launch another Claude instance (we are already running):

      cd /Users/michal/git/leadgen-pipeline
      git fetch origin staging
      bash scripts/worktree.sh bl1117-editable-company-name --no-claude

    If the script does not support --no-claude, fall back to manual:

      git worktree add .worktrees/bl1117-editable-company-name -b feature/bl1117-editable-company-name origin/staging

    Then cd into the worktree:

      cd /Users/michal/git/leadgen-pipeline/.worktrees/bl1117-editable-company-name

    Verify the branch is based off origin/staging (not main, not the current
    fix/bl1200-contact-source-enum branch):

      git branch --show-current        # must print: feature/bl1117-editable-company-name
      git log --oneline -1 origin/staging  # capture HEAD sha of staging
      git log --oneline -1                  # this branch's HEAD must equal staging HEAD initially

    Push the empty branch with upstream tracking:

      git push -u origin feature/bl1117-editable-company-name

    From here on, ALL subsequent tasks in plans 12-02..12-09 run from
    inside this worktree at .worktrees/bl1117-editable-company-name/.
    The main worktree's branch (fix/bl1200-contact-source-enum) must
    not be touched.
  </action>
  <verify>
    <automated>
      cd /Users/michal/git/leadgen-pipeline/.worktrees/bl1117-editable-company-name
      test "$(git branch --show-current)" = "feature/bl1117-editable-company-name"
      git ls-remote --heads origin feature/bl1117-editable-company-name | grep -q feature/bl1117-editable-company-name
    </automated>
  </verify>
  <done>
    Worktree exists at .worktrees/bl1117-editable-company-name, on branch
    feature/bl1117-editable-company-name based on origin/staging HEAD,
    and the branch is pushed to origin with upstream tracking.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write spec at docs/specs/editable-company-name.md and commit</name>
  <read_first>
    - /Users/michal/git/leadgen-pipeline/.planning/phases/12-allow-edit-company-name-both-inline-and-from-company-detail-/12-CONTEXT.md (CANONICAL — every implementation decision is locked here)
    - /Users/michal/git/leadgen-pipeline/docs/specs/ (list dir; mirror the format of existing specs, e.g. campaign-analytics.md if it exists)
  </read_first>
  <files>docs/specs/editable-company-name.md</files>
  <action>
    Inside the worktree at .worktrees/bl1117-editable-company-name, create
    docs/specs/editable-company-name.md with these EXACT section headings
    (verbatim — downstream gates grep for them):

      # Editable Company Name + Duplicate Detection
      ## Backlog
      ## Problem
      ## User Stories
      ## Acceptance Criteria
      ## Data Model Changes
      ## API Contracts
      ## Modal Wireframe
      ## Out of Scope (Deferred)
      ## Test Plan

    Content rules (do NOT redesign — copy from CONTEXT.md, expanded into
    spec prose):

    ### Backlog
      - Short ID + sprint name + URL to backlog dashboard
        `https://backlog.visionvolve.com/leadgen-pipeline/`

    ### Problem
      One paragraph: company-name typos and inconsistent legal-suffix
      capitalization mean duplicates accumulate in the LCC tenant; today
      operators cannot fix a name from the table or the detail page
      because no inline-edit affordance exists.

    ### User Stories
      - As an operator, I can rename a company from the Companies table.
      - As an operator, I can rename a company from its detail page.
      - As an operator, when I save a name that already exists in my
        tenant after normalization, I am prompted to merge / use the
        existing one / keep both / cancel.

    ### Acceptance Criteria (Given/When/Then — copy verbatim into the spec)
      AC-1 Given a company "Foo Bar s.r.o." exists in tenant T,
           When an operator renames "Foo Bar" to "Foo Bar s.r.o." in tenant T,
           Then PATCH returns 409 with body {code:"duplicate_company_name", matches:[{id,name,domain,status,owner,contact_count,last_activity_at}]}.
      AC-2 Given the 409 prompt is shown,
           When the operator clicks "Use this one" on a match,
           Then the modal closes, the input reverts to its pre-edit value, and the page navigates to /companies/{match.id}.
      AC-3 Given the 409 prompt is shown,
           When the operator clicks "Merge into this one",
           Then POST /api/companies/{edited.id}/merge?into={match.id} executes; on success the user lands on the surviving record's detail page and the edited record is hard-deleted.
      AC-4 Given the 409 prompt is shown,
           When the operator clicks "Keep both as separate",
           Then PATCH is retried with ?confirm_duplicate=keep_both and succeeds; an audit row with field_name='name' and metadata note duplicate_kept_intentionally is written.
      AC-5 Given a name normalizes to empty,
           When the operator submits,
           Then PATCH returns 400 with an empty-name error and no DB write happens.
      AC-6 Given two tenants T1 and T2 each have a company "Acme s.r.o.",
           When an operator in T1 renames an unrelated company to "Acme s.r.o.",
           Then only T1's existing Acme appears as a match (cross-tenant isolation).
      AC-7 Given a merge succeeds,
           When you query the surviving company,
           Then every contact / company_tags / enrichment_l1 / enrichment_l2 / company_tag_assignments / company_enrichment_market / company_enrichment_opportunity / company_enrichment_profile / company_enrichment_signals / company_legal_profile / company_news / company_registry_data / company_insolvency_data / strategy_documents row that pointed at the deleted company now points at the surviving company.

    ### Data Model Changes
      - companies.normalized_name TEXT NULL — populated by app.
      - index idx_companies_tenant_normalized_name (tenant_id, normalized_name) NON-UNIQUE.
      - contact_field_changes.metadata JSONB DEFAULT '{}' (added if not present).

    ### API Contracts
      - PATCH /api/companies/{id}  (existing)
          Body: { name?: string, ... }
          Query: ?confirm_duplicate=keep_both (optional, only meaningful when name changes)
          200 on success, 400 on empty-after-normalize, 404 on cross-tenant, 409 on collision:
          {
            "code": "duplicate_company_name",
            "matches": [
              { "id": uuid, "name": string, "domain": string|null,
                "status": string|null, "owner": {id,name}|null,
                "contact_count": int, "last_activity_at": iso8601|null }
            ]
          }
      - POST /api/companies/{id}/merge?into={surviving_id}  (NEW)
          RBAC: editor; both companies must be in caller's tenant.
          200: returns surviving company payload (same shape as GET /api/companies/{id}).
          400 if cross-tenant or invalid uuid; 404 if either id missing.

    ### Modal Wireframe (ASCII)
      ┌──────────────────────────────────────────────────────────┐
      │ Company name already exists                              │
      │ We found N companies with normalized name "{norm}".      │
      │                                                          │
      │ ┌─ {match.name}  ({domain})  ────────────────────────┐  │
      │ │  Owner: 🟢 {owner_name}   Status: {status}         │  │
      │ │  {contact_count} contacts · Last active {date}     │  │
      │ │  [Use this one]              [Merge into this one] │  │
      │ └────────────────────────────────────────────────────┘  │
      │                ... one card per match ...                │
      │                                                          │
      │           [Keep both as separate]   [Cancel]             │
      └──────────────────────────────────────────────────────────┘

    ### Out of Scope (Deferred)
      Copy the bullet list from CONTEXT.md <deferred> section verbatim.

    ### Test Plan
      Reference docs/testing/sprint-25-manual-tests.md (Plan 08 will
      add the manual test scenarios) and the integration tests listed
      in Plans 03 + 04.

    Commit and push:
      cd /Users/michal/git/leadgen-pipeline/.worktrees/bl1117-editable-company-name
      git add docs/specs/editable-company-name.md
      git commit -m "docs(BL-1117): add Phase 12 editable company name spec"
      git push origin feature/bl1117-editable-company-name
  </action>
  <verify>
    <automated>
      cd /Users/michal/git/leadgen-pipeline/.worktrees/bl1117-editable-company-name
      test -f docs/specs/editable-company-name.md
      grep -q "^## Acceptance Criteria" docs/specs/editable-company-name.md
      grep -q "^## API Contracts" docs/specs/editable-company-name.md
      grep -q "duplicate_company_name" docs/specs/editable-company-name.md
      grep -q "AC-1" docs/specs/editable-company-name.md
      grep -q "AC-7" docs/specs/editable-company-name.md
      git log -1 --oneline | grep -q "BL-1117"
    </automated>
  </verify>
  <done>
    docs/specs/editable-company-name.md committed and pushed; spec contains
    all 9 required sections and acceptance criteria AC-1..AC-7.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → backlog MCP | Operator invokes backlog_create_item with content that becomes a public dashboard row |
| operator → git remote | Pushing a feature branch to origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | I (Info Disclosure) | backlog_create_item description | accept | Description is intentionally public to the backlog dashboard; no secrets in spec text. |
| T-12-02 | T (Tampering) | git remote push | mitigate | Branch is feature/* — protected branches (main, staging) cannot be force-updated by the agent per CLAUDE.md hard rules. |
| T-12-03 | E (Elevation) | worktree path traversal | mitigate | Worktree path is parameterized from a fixed slug; no user input fed into the script. |
</threat_model>

<verification>
- Backlog item BL-1117 (or returned short_id) exists with status='Building' and is claimed by this agent.
- Worktree `.worktrees/bl1117-editable-company-name` exists on branch `feature/bl1117-editable-company-name`.
- Branch pushed to origin with upstream tracking.
- `docs/specs/editable-company-name.md` committed; contains all 9 named sections plus AC-1..AC-7.
</verification>

<success_criteria>
- `backlog_get_item(BL-1117)` returns status=Building and claimed_by!=null.
- `git -C .worktrees/bl1117-editable-company-name branch --show-current` prints `feature/bl1117-editable-company-name`.
- `git ls-remote --heads origin feature/bl1117-editable-company-name` returns one ref.
- `grep -c "^## " docs/specs/editable-company-name.md` >= 9.
</success_criteria>

<output>
After completion, create `.planning/phases/12-allow-edit-company-name-both-inline-and-from-company-detail-/12-01-SUMMARY.md` recording:
- Returned backlog short_id (BL-1117 expected)
- Branch name and origin sha
- Spec file path
- Any deviation from CONTEXT.md decisions
</output>
