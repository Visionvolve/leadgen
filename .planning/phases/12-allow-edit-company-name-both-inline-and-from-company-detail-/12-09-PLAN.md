---
phase: 12-allow-edit-company-name-both-inline-and-from-company-detail-
plan: 09
type: execute
wave: 7
depends_on: [12-08]
files_modified: []   # this plan opens the PR; no source edits
autonomous: false   # human gate at the very end -- user signs off staging before Done
requirements:
  - BL-1117
must_haves:
  truths:
    - "make test-changed exits 0 in the worktree on the latest commit"
    - "make lint-changed exits 0 in the worktree on the latest commit"
    - "cd frontend && npx tsc --noEmit exits 0"
    - "All Phase 12 commits exist on origin/feature/bl1117-editable-company-name"
    - "A PR is opened against origin/staging (NOT origin/main) with title 'feat(BL-1117): editable company name with duplicate detection'"
    - "BL-1117 status set to 'PR Open' with pr URL recorded"
    - "Staging migrations 073 + 074 are scheduled via gh workflow run migrate-staging in the PR body"
  artifacts:
    - path: "(remote) GitHub PR against staging"
      provides: "Phase 12 PR awaiting CI + 1 approval"
  key_links:
    - from: "feature/bl1117-editable-company-name"
      to: "PR against staging"
      via: "gh pr create --base staging"
      pattern: "gh pr create.*--base staging"
---

<objective>
Open the PR for Phase 12 against the staging branch (NOT main, per
CLAUDE.md hard rules), update BL-1117 to PR Open, and orchestrate the
staging migration. The final task is HUMAN-GATED: BL-1117 is only
moved to Done after the user signs off on staging validation
(T12.1..T12.10 from Plan 12-08).

Purpose: Definition-of-Done from CLAUDE.md requires PR to staging with
passing CI + 1 approval; the backlog bridge requires PR Open -> Done
status transitions; the schema migrations cannot run on staging until
the workflow is dispatched.
Output: One PR, two backlog status updates, one workflow dispatch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/12-allow-edit-company-name-both-inline-and-from-company-detail-/12-CONTEXT.md
@docs/specs/editable-company-name.md
@docs/testing/sprint-25-manual-tests.md

<interfaces>
From GitHub CLI (gh):
- gh pr create --base staging --head feature/bl1117-editable-company-name --title "..." --body "..."
- gh workflow run migrate-staging.yml  -- dispatches the staging migration

From backlog MCP (from CLAUDE.md):
- backlog_update_item(<short_id>, status="PR Open", pr=<url>)
- backlog_update_item(<short_id>, status="Done")
- backlog_release_item(<short_id>)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight checks (tests + lint + tsc) and last commit push</name>
  <read_first>
    - /Users/michal/git/leadgen-pipeline/CLAUDE.md ("Local Verification" + "Pre-PR Quality Gates" sections)
  </read_first>
  <files>(no source files; verification only)</files>
  <action>
    cd /Users/michal/git/leadgen-pipeline/.worktrees/bl1117-editable-company-name
    git status                                # must be clean
    git fetch origin staging
    git rebase origin/staging || { echo "REBASE FAILED -- resolve and re-run"; exit 1; }
    cd frontend && npx tsc --noEmit            # must pass
    cd ..
    make lint-changed                          # must pass
    make test-changed                          # must pass
    git push --force-with-lease origin feature/bl1117-editable-company-name
  </action>
  <verify>
    <automated>
      cd /Users/michal/git/leadgen-pipeline/.worktrees/bl1117-editable-company-name
      test -z "$(git status --porcelain)"
      git rev-parse HEAD > /tmp/head.txt
      git rev-parse origin/feature/bl1117-editable-company-name > /tmp/remote.txt
      diff /tmp/head.txt /tmp/remote.txt
    </automated>
  </verify>
  <done>
    Local HEAD == remote HEAD; all three gate commands (tsc, lint, tests)
    pass clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Open PR to staging</name>
  <read_first>
    - /Users/michal/git/leadgen-pipeline/docs/specs/editable-company-name.md (use the spec as the PR description source)
    - /Users/michal/git/leadgen-pipeline/CLAUDE.md ("Branch Model" -- PRs land on staging not main)
  </read_first>
  <files>(gh CLI; no local files modified)</files>
  <action>
    From the worktree:

      cd /Users/michal/git/leadgen-pipeline/.worktrees/bl1117-editable-company-name
      gh pr create --base staging \
                   --head feature/bl1117-editable-company-name \
                   --title "feat(BL-1117): editable company name with duplicate detection" \
                   --body "$(cat <<'PRBODY'
      ## Summary
      Phase 12 of milestone v25. Ships editable company name (inline in
      Companies table + on Company detail page) with server-side
      duplicate detection. On collision after normalization (trim +
      lowercase + diacritic strip + Czech/DE/EN legal-suffix strip),
      the backend returns 409 with match summaries; the frontend opens
      DuplicateCompanyModal offering per-match "Use this one" / "Merge
      into this one", plus footer "Keep both as separate" / "Cancel".

      Spec: docs/specs/editable-company-name.md
      Backlog: BL-1117 (Sprint 25 -- LCC client requests)
      GSD phase: 12-allow-edit-company-name-both-inline-and-from-company-detail-

      ## What changed
      ### Backend
      - `migrations/073_companies_normalized_name.sql` -- normalized_name TEXT column + non-unique index on (tenant_id, normalized_name).
      - `migrations/074_contact_field_changes_metadata.sql` -- metadata JSONB on audit table.
      - `api/services/name_normalize.py` -- pure normalize_company_name() with CZ/DE/EN suffix support.
      - `api/services/dedup.py` -- find_name_collisions() + merge_companies() with FOR UPDATE locking.
      - `api/routes/company_routes.py` -- PATCH duplicate gate; POST /api/companies/<id>/merge endpoint.
      - `scripts/backfill_normalized_name.py` -- idempotent backfill.

      ### Frontend
      - `frontend/src/config/companyColumns.tsx` -- name_edit column ON by default.
      - `frontend/src/hooks/useInlineEdit.ts` -- 409 duplicate_company_name dispatcher.
      - `frontend/src/hooks/useCompanyDuplicateGate.ts` -- shared state machine.
      - `frontend/src/components/companies/DuplicateCompanyModal.tsx` -- merge / use existing / keep both / cancel UI.
      - `frontend/src/components/ui/DetailField.tsx` -- new EditableText.
      - `frontend/src/pages/companies/CompanyDetail.tsx` -- editable name header.

      ### Tests
      - Backend unit + integration tests for normalize, dedup, PATCH duplicate gate, merge endpoint, full PATCH-409 -> merge cycle.
      - Frontend component test for DuplicateCompanyModal.
      - Playwright spec queued for sprint-completion E2E run.
      - Manual test script: docs/testing/sprint-25-manual-tests.md Phase 12 section (T12.1..T12.10).

      ## Migration plan
      Staging migrations 073 + 074 will be applied via
      `gh workflow run migrate-staging.yml` (see Task 3 of plan 12-09).
      Production migrations happen after staging sign-off, via the
      staging -> main PR triggering the prod migrate workflow.

      ## Test plan
      - [ ] CI green (lint + unit tests + tsc)
      - [ ] 1 approving review
      - [ ] After merge: GHA auto-deploys to staging-root
      - [ ] Run `gh workflow run migrate-staging.yml` to apply 073 + 074
      - [ ] User executes docs/testing/sprint-25-manual-tests.md Phase 12 (T12.1..T12.10)
      - [ ] On sign-off: BL-1117 -> Done; release claim

      ## Threat model summary
      - Tenant isolation enforced via `WHERE tenant_id = :t` on every dedup + merge query.
      - Mass-assignment guard preserved (existing `allowed` set).
      - Merge wraps in transaction with `SELECT ... FOR UPDATE`; rollback on FK failure.
      - Audit row captures JSON snapshot of deleted company (recovery path).
      - No SQL injection: all values parameterized; FK table names are a hard-coded literal list.

      Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
      PRBODY
      )"

    Capture the returned PR URL.
  </action>
  <verify>
    <automated>
      cd /Users/michal/git/leadgen-pipeline/.worktrees/bl1117-editable-company-name
      PR_URL=$(gh pr view --json url -q .url 2>/dev/null)
      test -n "$PR_URL"
      # Confirm PR base is staging not main
      gh pr view --json baseRefName -q .baseRefName | grep -q "^staging$"
    </automated>
  </verify>
  <done>
    PR exists, targets staging (not main), title and body match the
    spec.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update BL-1117 -> PR Open + dispatch migrate-staging workflow</name>
  <read_first>
    - /Users/michal/git/leadgen-pipeline/CLAUDE.md ("GSD <-> Backlog Status Bridge" section)
  </read_first>
  <files>(backlog MCP + gh; no local files modified)</files>
  <action>
    Step A: capture the PR URL from `gh pr view --json url -q .url` and
    update the backlog item:

      backlog_update_item("BL-1117", status="PR Open", pr=<PR_URL>)

    Step B: dispatch the staging migration workflow (it picks up
    migrations 073 + 074 from the merged staging branch -- BUT we
    dispatch AFTER merge below). Document in the PR comments that this
    is the next step, and add a reminder task.

    Actually -- per CLAUDE.md, the migration runs AFTER the PR merges
    (staging branch contains the migration files only post-merge). So
    Step B for THIS task is: post a PR comment instructing the
    reviewer/merger to run:

      gh workflow run migrate-staging.yml

    AFTER merge. Add this as a comment:

      gh pr comment --body "After merging this PR, run \`gh workflow run migrate-staging.yml\` to apply migrations 073 + 074 against the staging DB. Then proceed with docs/testing/sprint-25-manual-tests.md T12.1..T12.10."
  </action>
  <verify>
    <automated>
      cd /Users/michal/git/leadgen-pipeline/.worktrees/bl1117-editable-company-name
      # backlog status assertion
      # (MCP tool call -- assert via TaskOutput or direct re-fetch)
      backlog_get_item("BL-1117") | grep -q "PR Open"   # adapt to actual MCP response shape
      gh pr view --comments | grep -q "migrate-staging"
    </automated>
  </verify>
  <done>
    BL-1117 status='PR Open' with pr URL; PR comment scheduled the
    staging migration.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: HUMAN -- merge PR, run staging migration, execute manual test script, sign off</name>
  <what-built>
    PR opened against staging. Migrations 073 + 074 and backfill script
    ready to run.
  </what-built>
  <how-to-verify>
    1. Reviewer approves the PR (1 approval required by branch protection).
    2. Reviewer or operator merges the PR to staging via GitHub UI or
       `gh pr merge --squash --auto`.
    3. After merge, run from any local terminal:
         cd /Users/michal/git/leadgen-pipeline
         git fetch origin staging
         gh workflow run migrate-staging.yml
       Wait for the workflow to complete (check via gh run watch).
    4. Run the backfill against staging once the migration is applied.
       Since LEADGEN_DATABASE_URL on staging is set inside the API
       container, dispatch via SSH+docker exec OR run the script
       container-side. The simplest path: open a one-off Python shell
       on the staging API container per CLAUDE.md staging-access flow:
         claude --staging-access
         ssh ec2-user@3.124.110.199
         docker exec -it leadgen-api-rev-latest python3 /app/scripts/backfill_normalized_name.py
       Expect the second run to report updated=0.
    5. Login to https://leadgen-staging.visionvolve.com/ as
       test@staging.local / staging123.
    6. Execute docs/testing/sprint-25-manual-tests.md scenarios
       T12.1..T12.10. Mark each PASS / FAIL.
    7. If any FAIL: file a defect comment on the PR, fix in the worktree,
       cycle back through Plan 12-09 Task 1 (rebase + lint + push) and
       update the PR. DO NOT mark BL-1117 Done.
    8. If all PASS: sign off in the PR comments with "Staging T12.1..T12.10
       all PASS".
  </how-to-verify>
  <resume-signal>
    Type "approved" once all 10 manual test scenarios PASS on staging
    root. The agent will then mark BL-1117 Done and release the claim.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: After user signs off -- BL-1117 -> Done + release claim</name>
  <read_first>
    - /Users/michal/git/leadgen-pipeline/CLAUDE.md (status transitions)
  </read_first>
  <files>(backlog MCP only)</files>
  <action>
    Only execute AFTER Task 4's resume-signal "approved":

      backlog_update_item("BL-1117", status="Done")
      backlog_release_item("BL-1117")

    Also update GSD state:

      Update .planning/STATE.md:
        - status: planning -> Phase 12 complete
        - completed_phases += 1
        - last_activity: "<today> -- Phase 12 (BL-1117) merged + validated on staging"

      Update .planning/ROADMAP.md Phase 12 row: [x] (checked).
      Update Phase 12 'Plans' list (line ~301): replace
        '- [ ] TBD -- define during /gsd-plan-phase 12'
      with
        '- [x] 12-01..12-09 (9 plans) -- shipped via BL-1117'

    Commit those `.planning/` updates to the MAIN repo (not the worktree;
    .planning lives on staging long-term):

      cd /Users/michal/git/leadgen-pipeline.worktrees/bl1117-editable-company-name
      git add .planning/STATE.md .planning/ROADMAP.md
      git commit -m "docs(BL-1117): mark Phase 12 complete in STATE + ROADMAP"
      git push origin feature/bl1117-editable-company-name
      # NOTE: this update could optionally be cherry-picked to staging
      # via a follow-up tiny PR; for now keep on the feature branch and
      # let the merge carry it.

    If the PR was already merged before this update, the update is now
    a follow-up commit on staging directly via a quick PR -- see
    backlog item BL-1117 history for context.
  </action>
  <verify>
    <automated>
      backlog_get_item("BL-1117") | grep -q "Done"
      grep -q "Phase 12" .planning/STATE.md
      grep -q "^- \\[x\\] \\*\\*Phase 12" .planning/ROADMAP.md
    </automated>
  </verify>
  <done>
    BL-1117 closed; claim released; GSD STATE + ROADMAP reflect Phase 12
    complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature branch -> staging | PR review + CI gate |
| staging -> production | Separate PR not covered by this plan (post-milestone activity) |
| migration workflow | GHA-only; agent never runs migrations against staging shell directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-42 | E (Unauthorized production deploy) | merge to main | mitigate | PR targets staging not main; CLAUDE.md forbids running deploy/deploy-*.sh; staging-to-main is a separate human-gated PR. |
| T-12-43 | T (Premature Done) | BL-1117 status | mitigate | Task 5 is gated behind Task 4 human checkpoint -- impossible to set Done without "approved" signal. |
| T-12-44 | I (Test data leakage on staging) | manual + Playwright tests | accept | Test rows are prefixed and torn down per Plan 12-08; staging is non-production data anyway. |
| T-12-45 | D (Migration race vs container restart) | migrate-staging workflow | mitigate | Migration files are additive (ADD COLUMN IF NOT EXISTS) -- safe if a container restart happens mid-run. |
| T-12-46 | R (PR description tampering) | gh pr create body | mitigate | PR body sourced from docs/specs/editable-company-name.md content + this plan; reviewer can diff against spec file in same PR. |
</threat_model>

<verification>
- `gh pr view --json baseRefName` returns "staging".
- BL-1117 status transitions PR Open -> Done occur in correct order.
- `.planning/STATE.md` and `.planning/ROADMAP.md` updated.
- Migration workflow dispatched (manual in Task 4); backfill ran on
  staging with second run reporting updated=0.
- Manual test script T12.1..T12.10 all PASS (user sign-off).
</verification>

<success_criteria>
1. PR open against staging with all CI green and 1 approval.
2. Staging migrations 073 + 074 applied + backfill complete.
3. User executes manual test script and signs off all 10 scenarios.
4. BL-1117 -> Done; claim released.
5. STATE + ROADMAP reflect Phase 12 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/12-allow-edit-company-name-both-inline-and-from-company-detail-/12-09-SUMMARY.md` recording:
- Final PR URL + merge commit sha.
- Migration workflow run URL.
- Staging backfill row count.
- Any T12.* scenarios that needed a re-cycle.
- Time-on-PR (open -> merged).
</output>
