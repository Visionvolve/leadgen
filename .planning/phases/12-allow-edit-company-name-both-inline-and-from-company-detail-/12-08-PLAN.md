---
phase: 12-allow-edit-company-name-both-inline-and-from-company-detail-
plan: 08
type: execute
wave: 6
depends_on: [12-05, 12-06, 12-07]
files_modified:
  - docs/testing/sprint-25-manual-tests.md
  - tests/unit/test_company_duplicate_full_flow.py
  - frontend/tests/e2e/company-duplicate-merge.spec.ts
autonomous: true
requirements:
  - BL-1117
must_haves:
  truths:
    - "docs/testing/sprint-25-manual-tests.md exists with a Phase 12 section covering all CONTEXT.md user flows"
    - "Integration test test_company_duplicate_full_flow.py exercises the full PATCH-409 -> merge -> survival cycle end-to-end on the backend"
    - "Playwright E2E spec exists (queued for sprint completion E2E run, NOT per-PR per CLAUDE.md)"
    - "The E2E spec is tagged so the sprint QA agent knows to include it"
  artifacts:
    - path: "docs/testing/sprint-25-manual-tests.md"
      provides: "Manual test script for the LCC user to run on staging root"
      contains: "## Phase 12: Editable Company Name"
    - path: "tests/unit/test_company_duplicate_full_flow.py"
      provides: "Backend integration covering PATCH 409 -> merge happy path"
    - path: "frontend/tests/e2e/company-duplicate-merge.spec.ts"
      provides: "Playwright spec for sprint-completion E2E run"
  key_links:
    - from: "Integration test"
      to: "PATCH /api/companies/<id> + POST /api/companies/<id>/merge"
      via: "Flask test client sequence"
      pattern: "patch.*api/companies.*then.*post.*merge"
---

<objective>
Lock in the testing layer that proves Phase 12 works end-to-end. Three
artifacts: (1) the sprint manual test script the user reads when
validating on staging root; (2) one backend integration test that
walks the whole PATCH-409 -> merge -> survival cycle (separate from the
per-handler unit tests already in Plans 12-03 + 12-04); (3) a Playwright
E2E spec queued for the sprint-completion run (NOT per-PR per CLAUDE.md
testing strategy).

Purpose: Manual test script is the deliverable the operator uses to
sign off. The integration test is the regression guard. The Playwright
spec is the sprint QA agent's evidence at the end of the sprint.
Output: 3 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-allow-edit-company-name-both-inline-and-from-company-detail-/12-CONTEXT.md
@CLAUDE.md
@docs/testing/   # check whether sprint-25-manual-tests.md already exists from earlier phases

<interfaces>
From api/routes/company_routes.py (shipped 12-03 + 12-04):
- PATCH /api/companies/<id>           (409 with matches)
- POST  /api/companies/<id>/merge?into=<surviving>

From frontend (shipped 12-05/06/07):
- Companies table inline-edit on name column
- CompanyDetail page-heading edit
- DuplicateCompanyModal at both entry points
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/testing/sprint-25-manual-tests.md Phase 12 section</name>
  <read_first>
    - /Users/michal/git/leadgen-pipeline/docs/testing/ (list; if sprint-25-manual-tests.md exists APPEND, do NOT overwrite earlier phases; if absent create)
    - /Users/michal/git/leadgen-pipeline/.planning/phases/12-allow-edit-company-name-both-inline-and-from-company-detail-/12-CONTEXT.md (every user-facing action)
  </read_first>
  <files>docs/testing/sprint-25-manual-tests.md</files>
  <action>
    If the file does NOT exist, create it with a top-level header:

      # Sprint 25 -- LCC Client Requests -- Manual Test Script
      ...
      (Phases 1..11 sections may be filled in by other phase plans;
      leave placeholders if they don't exist.)

    Append a Phase 12 section (always append; never overwrite existing
    content from prior phases):

      ## Phase 12: Editable Company Name + Duplicate Detection (BL-1117)

      Login URL: https://leadgen-staging.visionvolve.com/
      Test user: test@staging.local / staging123 (super_admin)
      Tenant: visionvolve (default after login)

      ### T12.1 Clean rename from Companies table
      1. Navigate to /visionvolve/companies/
      2. Confirm the table shows an inline-editable name column by default
         (pencil affordance on hover; the plain hyperlink column is hidden
         under the column picker).
      3. Pick any company with a unique name; click the cell, type a new
         unique name, press Enter.
      4. Expect: "Saving" -> "Saved" badge; refresh -> new name persists.
      PASS if all three steps green.

      ### T12.2 Empty-name rejection
      1. Same as T12.1 but type only whitespace then Enter.
      2. Expect: red error indicator on cell; original name remains.
      PASS if no PATCH 200 is observed (Network panel) and value reverts.

      ### T12.3 Duplicate-name modal appears
      1. Pick company A. Note its name.
      2. Pick company B; rename B to match A (e.g. "Acme s.r.o." if A is "Acme")
         -- normalization should collapse both to 'acme'.
      3. Press Enter.
      4. Expect: DuplicateCompanyModal opens. Card for A is visible with
         A's domain, status, owner, contact count, last activity date.
         Default focus is on "Use this one".
      PASS if modal renders with one card for A and the default focus
      is visible.

      ### T12.4 'Use this one' navigates and discards rename
      1. From T12.3 state, click "Use this one" on A's card.
      2. Expect: modal closes, B's cell value reverts to its prior value,
         URL changes to /visionvolve/companies/<A.id>.
      PASS if all three.

      ### T12.5 'Merge into this one' merges and lands on survivor
      1. From T12.3 state, click "Merge into this one" on A's card.
      2. Expect: modal shows "Merging...", then closes; URL changes to
         /visionvolve/companies/<A.id>; A's contact count has increased
         by B's prior contact count; B is gone from /visionvolve/companies/
         (the list does not contain B's old name).
      PASS if all three.

      ### T12.6 'Keep both as separate'
      1. From T12.3 state (recreate B since T12.5 deleted it), click footer
         "Keep both as separate".
      2. Expect: modal closes, B's name is saved (PATCH 200), both A and
         B appear in /visionvolve/companies/ with the same name.
      PASS if both rows survive.

      ### T12.7 'Cancel' / Esc reverts
      1. From T12.3 state, press Esc.
      2. Expect: modal closes, B's cell reverts to pre-edit value, no
         PATCH 200 observed.
      PASS if value matches pre-edit.

      ### T12.8 Detail-page edit
      1. Open /visionvolve/companies/<any.id>
      2. Hover the company name heading -- pencil should appear.
      3. Click pencil, change name, press Enter.
      4. Expect: heading updates; toast "Name saved" appears.
      PASS if heading reflects new value after refresh.

      ### T12.9 Detail-page duplicate flow
      1. From a Company detail page, rename to an existing in-tenant name.
      2. Expect: same DuplicateCompanyModal opens; same 4 actions work.
      PASS if modal renders.

      ### T12.10 Tenant isolation
      Requires a second tenant; this step is an operator/super-admin
      verification only. Skip with a note if no second tenant is
      reachable on staging.
      1. As super_admin, ensure tenant 'visionvolve' has 'Acme s.r.o.'
         and tenant 'losers' also has 'Acme s.r.o.' (or create test data).
      2. Login as a visionvolve user, attempt to rename another company
         to 'Acme'.
      3. Expect: the modal shows only the visionvolve Acme, never the
         losers Acme.
      PASS if losers' row is NOT in matches.

      ### After all green
      Notify the user: "Phase 12 staging validation complete -- all 10
      scenarios pass. Ready to merge staging -> main."

    Commit message format -- see Task 4.
  </action>
  <verify>
    <automated>
      cd /Users/michal/git/leadgen-pipeline/.worktrees/bl1117-editable-company-name
      test -f docs/testing/sprint-25-manual-tests.md
      grep -q "^## Phase 12: Editable Company Name" docs/testing/sprint-25-manual-tests.md
      grep -c "^### T12\\." docs/testing/sprint-25-manual-tests.md | grep -qE "^[0-9]+$"
      [ "$(grep -c '^### T12\\.' docs/testing/sprint-25-manual-tests.md)" -ge 10 ]
    </automated>
  </verify>
  <done>
    Phase 12 section appended with at least 10 scenarios (T12.1..T12.10).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test -- full PATCH-409 -> merge -> survival cycle</name>
  <read_first>
    - /Users/michal/git/leadgen-pipeline/tests/unit/test_company_patch_duplicate_gate.py (shipped 12-03 -- mirror fixture setup)
    - /Users/michal/git/leadgen-pipeline/tests/unit/test_company_merge_endpoint.py (shipped 12-04 -- mirror merge call pattern)
  </read_first>
  <files>tests/unit/test_company_duplicate_full_flow.py</files>
  <behavior>
    One scenario: tenant T has company B (name='Bar'); operator renames
    a separate company A (name='Foo') to 'Bar'. Expect PATCH -> 409.
    Operator picks "Merge into this one"; client calls
    POST /api/companies/<A.id>/merge?into=<B.id>. Expect 200.

    Final state:
      - companies row A is deleted.
      - companies row B still has name='Bar' and normalized_name='bar'.
      - All A's contacts now have company_id = B.id.
      - Audit log contains:
          * Maybe-zero PATCH audit rows on A (since PATCH was rejected 409)
          * One merge audit row with entity_id=B.id, field_name='merged_from',
            old_value=A.id, metadata.deleted_snapshot present.
  </behavior>
  <action>
    Write tests/unit/test_company_duplicate_full_flow.py implementing
    the scenario above. Use the same fixture conventions as the existing
    company-routes tests.

    Run `make test-changed` and expect it to PASS (the underlying
    handlers were shipped + green in 12-03 and 12-04 -- this is the
    composed verification, not a new feature).
  </action>
  <verify>
    <automated>
      cd /Users/michal/git/leadgen-pipeline/.worktrees/bl1117-editable-company-name
      make test-changed 2>&1 | tee /tmp/full.txt
      grep -E "test_company_duplicate_full_flow" /tmp/full.txt | grep -v FAIL
      ! grep -E "FAILED.*test_company_duplicate_full_flow" /tmp/full.txt
    </automated>
  </verify>
  <done>
    Full-flow integration test passes in one run.
  </done>
</task>

<task type="auto">
  <name>Task 3: Playwright E2E spec (queued for sprint completion)</name>
  <read_first>
    - /Users/michal/git/leadgen-pipeline/frontend/tests/e2e/ (list; pick an existing spec as the style template)
    - /Users/michal/git/leadgen-pipeline/CLAUDE.md ("Testing Strategy" section -- this spec must NOT run in per-PR CI)
  </read_first>
  <files>frontend/tests/e2e/company-duplicate-merge.spec.ts</files>
  <action>
    Create a Playwright spec that mirrors the existing spec style in
    frontend/tests/e2e/. Cover the happy path:

      1. Login as test@staging.local
      2. Navigate to /visionvolve/companies/
      3. Pick two test companies whose normalized names collide
         (set up via beforeAll using the API directly: POST two companies
         named 'PWTest Foo s.r.o.' and 'PWTest Foo'; teardown in afterAll).
      4. Click the name_edit cell of PWTest Foo, type 'PWTest Foo s.r.o.',
         press Enter.
      5. Assert: DuplicateCompanyModal visible. Card for PWTest Foo s.r.o.
         shows owner, contact count.
      6. Click 'Merge into this one'.
      7. Assert: URL is /visionvolve/companies/<surviving.id>; the
         deleted PWTest Foo row no longer in the list.

    Tag the spec for sprint-end-only execution (per CLAUDE.md "Testing
    Strategy"). Use whatever convention exists in
    frontend/tests/e2e/playwright.config.ts -- typically a tag like
    @sprint-end or a separate project name. If no such mechanism exists,
    place the file in a subdirectory frontend/tests/e2e/sprint-end/ and
    update the test plan note in the spec body referencing CLAUDE.md.

    DO NOT run the E2E spec here -- it runs in the sprint-completion
    QA pass (see CLAUDE.md "Testing Responsibilities").
  </action>
  <verify>
    <automated>
      cd /Users/michal/git/leadgen-pipeline/.worktrees/bl1117-editable-company-name
      test -f frontend/tests/e2e/company-duplicate-merge.spec.ts \
        || test -f frontend/tests/e2e/sprint-end/company-duplicate-merge.spec.ts
      grep -q "PWTest Foo" frontend/tests/e2e/**/company-duplicate-merge.spec.ts 2>/dev/null \
        || grep -q "PWTest Foo" frontend/tests/e2e/company-duplicate-merge.spec.ts
      cd frontend && npx tsc --noEmit
    </automated>
  </verify>
  <done>
    Playwright spec authored and type-checks; tagged for sprint-end
    execution.
  </done>
</task>

<task type="auto">
  <name>Task 4: Lint + commit + push</name>
  <read_first>none (git only)</read_first>
  <files>(git only)</files>
  <action>
    cd /Users/michal/git/leadgen-pipeline/.worktrees/bl1117-editable-company-name
    cd frontend && npx tsc --noEmit
    cd ..
    make lint-changed
    make test-changed   # full flow integration must still pass
    git add docs/testing/sprint-25-manual-tests.md \
            tests/unit/test_company_duplicate_full_flow.py \
            frontend/tests/e2e/   # add the spec
    git commit -m "test(BL-1117): manual test script + full-flow integration + Playwright spec"
    git push origin feature/bl1117-editable-company-name
  </action>
  <verify>
    <automated>
      cd /Users/michal/git/leadgen-pipeline/.worktrees/bl1117-editable-company-name
      git log -1 --oneline | grep -q "BL-1117"
      test -z "$(git status --porcelain)"
    </automated>
  </verify>
  <done>
    Plan 12-08 changes committed and pushed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures | Created/destroyed inside test scope; never persisted |
| Playwright spec data | beforeAll/afterAll lifecycle handles cleanup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-39 | T (Test data pollution on staging) | Playwright PWTest Foo rows | mitigate | beforeAll inserts under 'PWTest Foo' prefix; afterAll deletes by prefix. Prefix is unique so no production data is at risk. |
| T-12-40 | I (Manual test script reveals tenant slugs) | sprint-25-manual-tests.md | accept | Tenant slugs ('visionvolve', 'losers') already public in CLAUDE.md and elsewhere in docs/. Same trust level. |
| T-12-41 | R (Audit log claims unverifiable) | full-flow integration | mitigate | Test asserts the merged_from audit row AND the deleted_snapshot contents -- failure surfaces immediately. |
</threat_model>

<verification>
- docs/testing/sprint-25-manual-tests.md contains a Phase 12 section
  with >= 10 scenarios.
- test_company_duplicate_full_flow.py passes under `make test-changed`.
- Playwright spec authored and tsc clean (NOT executed here).
- Plan committed and pushed.
</verification>

<success_criteria>
1. The manual test script is sufficient for the user to validate Phase
   12 on staging root without operator hand-holding.
2. The full-flow integration test passes in one run (regression guard).
3. The Playwright spec is ready for sprint-end execution.
</success_criteria>

<output>
After completion, create `.planning/phases/12-allow-edit-company-name-both-inline-and-from-company-detail-/12-08-SUMMARY.md` recording:
- Whether sprint-25-manual-tests.md was created or appended-to.
- Total number of T12.* scenarios in the script.
- Playwright spec final path + tagging mechanism used.
</output>
